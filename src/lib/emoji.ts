import { DEFAULT_TAG_EMOJI } from '../../shared/emoji'

export { DEFAULT_TAG_EMOJI }

/** タグに付けられる絵文字。作成と名前の付け直しで同じものを出す。 */
export const EMOJI = [DEFAULT_TAG_EMOJI, '📘', '🧴', '🍳', '🏃', '🎸', '🐾', '✏️']
